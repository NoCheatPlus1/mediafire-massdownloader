# mediafire-massdownloader
This is a simple python script that automatically massdownloads mediafire files without needing premium

Requires tpdm and requests



python -m pip install tqdm
python -m pip install requests

python main.py
